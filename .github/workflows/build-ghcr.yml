name: Build and Push PHP Images

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - Dockerfile
      - Caddyfile
      - php.ini
      - .github/workflows/build-ghcr.yml

permissions:
  contents: read
  packages: write

env:
  IMAGE_REPO: ghcr.io/leaflownet/php

jobs:
  build:
    name: Build php${{ matrix.php_version }}
    runs-on: leaflow-php
    strategy:
      fail-fast: false
      matrix:
        include:
          - php_version: "8.3"
            frankenphp_tag: "1.3-php8.3-bookworm"
          - php_version: "8.4"
            frankenphp_tag: "1.3-php8.4-bookworm"
          - php_version: "8.5"
            frankenphp_tag: "php8.5-bookworm"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME || github.repository_owner }}
          password: ${{ secrets.GHCR_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.IMAGE_REPO }}:php${{ matrix.php_version }}
          build-args: |
            FRANKENPHP_TAG=${{ matrix.frankenphp_tag }}
