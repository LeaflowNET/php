{
	skip_install_trust

	{$CADDY_GLOBAL_OPTIONS}

	frankenphp {
		{$FRANKENPHP_CONFIG}
	}
}

{$CADDY_EXTRA_CONFIG}

{$SERVER_NAME:localhost} {
	root {$SERVER_ROOT:public/}
	encode zstd br gzip

	@builtinHealth path {$DEFAULT_HEALTHCHECK_PATH:/__builtin_healthcheck_disabled__}
	respond @builtinHealth "ok" 200

	{$CADDY_SERVER_EXTRA_DIRECTIVES}

	php_server
}

import /etc/caddy/Caddyfile.d/*.caddyfile
